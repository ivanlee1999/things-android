<?xml version="1.0" encoding="utf-8"?>
<!--
  Release builds speak HTTPS only. The backend this app talks to answers for a whole Things
  Cloud account behind a single bearer key, so cleartext to anything but the loopback would put
  that key on the wire. The intended path is a Cloudflare Tunnel with an Access service token.

  A `domain` matches one hostname or IP literal exactly — there is no CIDR syntax — so "the LAN"
  cannot be expressed here. The LAN case is served by the debug build instead: app/src/debug has
  an override permitting cleartext, and the preview ("next") APK is a debug build. UrlPolicy then
  refuses an http:// URL whose host is not private even there, so the rule is not simply off.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
